{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run kube-config command",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "args": ["kube-config"],
      "env": {
        "AWS_PROFILE": "gbh",
        "AWS_REGION": "us-east-1"
      }
    }
  ]
}
